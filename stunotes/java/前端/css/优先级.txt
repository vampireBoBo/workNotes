1、css选择器
	id class 标签元素 过滤等
	
2、类型
	行内(定义在元素中使用style属性等)  内联(定义在页面的style标签内)   外嵌（引入的定义css样式的外部文件）
	
3、important>id>class>元素 ====（剩余皆为同级）

		针对于同级样式 或 多个class中的冲突样式  那个样式中的重复属性在最后面加载那个就生效  
		【注意：所谓的加载是样式的定义语句的加载  而不是 标签中引入多个class的顺序】